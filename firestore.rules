rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads and writes for development purposes.
      // WARNING: This is insecure for a production environment.
      allow read, write: if true;
    }
  }
}
